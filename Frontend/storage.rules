rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow any user to upload reports
      allow read: if true;
      allow write: if true;
    }
  }
}
